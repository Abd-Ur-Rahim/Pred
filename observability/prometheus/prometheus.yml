global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # add other services here as team builds them
  # - job_name: 'ingestion-service'
  #   static_configs:
  #     - targets: ['ingestion-service:8000']